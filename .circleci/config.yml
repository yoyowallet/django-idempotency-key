version: 2.1
#orbs:
#  codecov: codecov/codecov@1.0.1
jobs:
  build:
    docker:
      - image: circleci/redis
      - image: circleci/python:3.6
    steps:
      - checkout
      - run: apt install python3-pip
      - run: pip install --no-cache-dir pipenv && pip list && pipenv install --dev
      - run: pip list
      - run: pipenv run -- make test
      #- codecov/upload:
      #    file: htmlcov/
